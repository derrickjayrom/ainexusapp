class AppRouteNames {
  static const onboarding = '/onboarding';
  static const login = '/login';
  static const app = '/app';
  static const saved = '/app/saved';
}

class ShellArgs {
  final int initialIndex;
  const ShellArgs({required this.initialIndex});
}
