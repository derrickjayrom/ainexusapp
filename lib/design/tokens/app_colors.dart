import 'package:flutter/material.dart';

class AppColors {
  // Base
  static const bgTop = Color(0xFF070B14);
  static const bgMid = Color(0xFF0B1323);
  static const bgBottom = Color(0xFF09101E);

  // Surfaces (cards/inputs)
  static const surface = Color(0xFF141C2B);
  static const surface2 = Color(0xFF101827);
  static const stroke = Color(0xFF273247);

  // Text
  static const textPrimary = Color(0xFFEFF4FF);
  static const textSecondary = Color(0xFF9BAAC2);
  static const textMuted = Color(0xFF6C7B93);

  // Accent
  static const primary = Color(0xFF1D5CFF);
  static const primarySoft = Color(0xFF2A6BFF);

  // Status
  static const danger = Color(0xFFFF4D4D);
}
